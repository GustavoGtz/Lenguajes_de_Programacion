#lang racket

(define open-paren-regex ...)

(define close-paren-regex ...)

(define define-regex ...)

(define sum-regex ...)

(define mult-regex ...)

(define identifier-regex ...)

(define number-regex ...)

(provide open-paren-regex
         close-paren-regex
         define-regex
         sum-regex
         mult-regex
         identifier-regex
         number-regex)
